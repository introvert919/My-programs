# Уровень 1.3
# 1. Дана строка. Если в этой строке более одного символа, выведите в консоль предпоследний символ этой строки.

string = input()

length = len(string)

if length >1:
    print(string[-2])

# 2. Даны два числа. Проверьте что первое число без остатка делится на второе.

a = int(input())
b = int(input())

if a%b == 0:
    print("Делится без остатка")
else:
    print("Не делится без остатка")

# 3. Дана строка 'abcde'. Получите список ее символов ['a','b','c','d','e'].

string = 'abcde'

list = []
for i in string:
    list.append(i)

print (list)

# 4. Дан список [1,2,3,4,5,6]. Получите из него следующий срез [3,4,5].

list = [1,2,3,4,5,6]

result = list[2:5:1]

print(result)

# 5. Дан словарь с датой. 
# {
#  'year' : '2025',
#  'month' : '12',
#  'day' : '31'
# }
# Из элементов словаря соберите дату в следующем формате '2025-12-31'.

slovar = {'year' : '2025',
          'month' : '12',
          'day' : '31'}

data = slovar['year'] + "-" + slovar['month'] + "-" + slovar['day']

print (data)
