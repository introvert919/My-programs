# Уровень 2. Уровень 2.1
# 1. Дан спсисок со строками ['строка 1','строка 2','http://строка 3','строка 1','строка 2','http://строка 3']. 
# Осттавьте в этом списке только те строки, которые начинаются на http://.

list = ['строка 1','строка 2','http://строка 3','строка 4','строка 5','http://строка 6']

find = 'http://'

for i in list:
    if find in i:
        list.remove(i)
        
print(list)

# 2. Дана строка . Найдите позицию первого нуля в строке.

string = 'abcd0ef0gh0ijk'

result = string.find('0')

print(result)

# 3. Дан список ['a','b','1','c','1','d']. Удалите из него все элементы со значением '1'.

list = ['a','b','1','c','1','d']

for i in list:
    if i == '1':
        list.remove(i)
        
print(list)

# 4. Выведите в консоль все числа в промежутке от 10 до 1000, сумма первой и второй цифры которых равна пяти.

n = 10

while n <=1000:
    
    string = str(n)
    a = int (string[0])
    b = int (string[1])
    
    if a+b ==5:
        print(n)
        
    n += 1
        
# 5. Дана строка 'abcdeabc'. Очистите ее от дублей символов.

string = 'abcdeabc'

result = set(string)

print(result)

