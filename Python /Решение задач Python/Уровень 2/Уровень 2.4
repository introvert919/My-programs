# Уровень 2.4
# 1. Дана строка с буквами и цифрами 'abc5d6ef78gh98ijk'. Получите позицию перой цифры в этой строке.

string = 'abc5d6ef78gh98ijk'

for i in string:
    if i == '0' or i =='1' or i=='2' or i=='3' or i=='4' or i=='5' or i=='6' or i=='7' or i=='8' or i=='9':
        a = string.find(i)
        print(a)
        break

# 2. Дано число 234654345656. Выведите в консоль количество четных цифр в этом числе.

a = 234654345656
string = str(a)
list = []
for i in string:
   list.append(i)

count = 0

for i in list:
   if int(i)%2==0:
      count += 1
      
print(count)

# 3. Дан словарь {'a':1, 'b':2, 'c':3, 'd':4}. Получите список его ключей ['a', 'b', 'c', 'd'].

dict = {'a':1, 'b':2, 'c':3, 'd':4}

list = dict.keys()

print(list)

# 4. Дана строка 'abcde'. Переведите в верхний регистр все нечетные буквы этой строки 'AbCdE'.

string = 'abcde'

count = 0

result =""

for i in string:
    count += 1
    if count%2 != 0:
        a = i.upper()
        result += a
    else:
        result += i

print(result)

#  5. Дана срока со словами 'aaa bbb ccc'. Сделайте заглавными первый символ каждого слова в этой строке 'Aaa Bbb Ccc'.

string = 'aaa bbb ccc'

list = string.split(' ')

result = ""

for i in list:
    a = str.title(i)
    result += a +" "
   
print(result)

# 6. Дана дата в следующем формате '2025-12-31'. Преобразуйте эту дату в следующий кортеж ('31','12', '2025').

string = '2025-12-31'

list1 = string.split('-')
list = list1[::-1]

result = tuple(list)
     
print(result)
