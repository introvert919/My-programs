# Уровень 2.2
# 1. Дан список с числами [1,4,-7,8,-4,3,-1,5]. Посчитайте количество отрицательных чисел в этом списке.

list = [1,4,-7,8,-4,3,-1,5]

result = 0

for i in list:
    if i<0:
        result += 1
        
print(result)

# 2. Дан список с числами. [1,4,-7,8,-4,3,-1,5]. Оставьте в нем только положительные числа.

list = [1,4,-7,8,-4,3,-1,5]

for i in list:
    if i<0:
        list.remove(i)
        
print(list)

# 3. Дана строка 'abcdef'. Удалите предпоследний символ этой строки.

string = 'abcdef'

list = []

for i in string:
    list.append(i)

del list[-2]

result = ''.join(list)

print(result)

# 4. Дан список со строками ['строка 1','строка 2.html','строка 3.html','строка 4','строка 5.html','строка 6','строка 7','строка 8']. 
# Оставьте в эттом списке только те строки, которые заканчиваются на ".html".

list = ['строка 1','строка 2.html','строка 3.html','строка 4','строка 5.html','строка 6']

for i in list:
    if not '.html' in i:
        list.remove(i)
        
print(list)

# 5. Дaн список с дробями [1.456, 2.125, 3.32, 4.1, 5.34]. 
# Округлите эти дроби до одного знака в дробной части.

list = [1.456, 2.125, 3.32, 4.1, 5.34]

for i in list:
    
    print(round(i, 1))

# 6. Дан словарь {'a':1, 'b':2, 'c':3, 'd':4}. Получите список его значений.

dict = {'a':1, 'b':2, 'c':3, 'd':4}

print(dict.values())
