# Уровень 3.9
# 1. Дан список с числами [2, 30, 56, 400, 7000, 45, 67, 80000]. Удалите из него числа, имеющие два и более нуля.

list = [2, 30, 56, 400, 45, 67, 8050]

for i in list:
    a = str(i)
    count1 = a.count("0")
    if count1 >= 2:
        list.remove(i)   
        
print(list)

# 2. Найдите все числа от 1 до 1000, сумма цифр которых равна 13. Результат запишите в сет.

list = []

n = 0
result = []
sum = 0

while n <=1000:
    list.append(n)
    n += 1
    
for i in list:
    string = str(i)
    for i in string:
        sum += int(i)
        
    if sum == 13:
        result.append(int(string))
   
    sum = 0
     
print(result)

# 3. Дан список [1, 2, 3]. Сделайте так, чтобы в нем каждый элемент повторился два раза [1, 1, 2, 2, 3, 3].

list =[1, 2, 3]

result = []

for i in list:
    result.append(i)
    result.append(i)
    
print(result)

# 4. Даны два списка list1 = [1,2,3], list2 = [4,5,6]. 
# Выведите в консоль с помощью одного цикла попарно значения этих списков "1,4", "2,5", "3,6".

list1 = [1,2,3]
list2 = [4,5,6]

list3 = list1+list2

length = len(list1)

n = 0

for i in list3:
    print(i, i+length)
    if i == length:
        break
